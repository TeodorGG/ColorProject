import {AppRegistry} from 'react-native';
import App from '../../src/App';

AppRegistry.registerComponent("Color", () => App);
AppRegistry.runApplication("Color", {
       rootTag: document.getElementById('root'),
     });

